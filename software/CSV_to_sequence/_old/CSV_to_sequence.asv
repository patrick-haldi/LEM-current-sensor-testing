clc
clear all
close all

filename = 'profile.csv';

disp('Konvertierung gestartet!')
data = dlmread([pwd '\CSV_in\' filename], ';', 0, 1);
data_to_sequences(data);
disp('Konvertierung abgeschlossen!')